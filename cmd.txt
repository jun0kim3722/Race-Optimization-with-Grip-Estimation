# get permission back
sudo chown -R 1000:1000 /home/j0k/Project/ROEG/roeg_ws/src/roeg

# build from ws
rm -rf install build log
rosdep update
rosdep install -i --from-path src --rosdistro galactic -y
colcon build

# build DockerImage
docker build -t ros2-roeg .

# run program
source install/setup.bash
ros2 run roeg get_friction_map
or
ros2 run roeg get_friction_map <track name>

# run multi-terminal
docker exec -it <name or #id> bash
source install/setup.bash